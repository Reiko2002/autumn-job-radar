# 秋招雷达 v0

## 产品目标

为 2027 届、关注 AI 产品/产品管培/教育科技方向的个人求职者，提供一个无需账号、无需付费服务即可使用的秋招岗位发现与投递管理工具。

## v0 核心流程

1. 在岗位广场按关键词、方向、城市和匹配度筛选演示岗位。
2. 查看规则生成的匹配分、匹配理由和能力缺口。
3. 收藏岗位，或加入投递流程。
4. 在看板中更新待投递、已投递、笔试、面试、Offer、未通过状态。
5. 修改求职偏好后立即重算排序；数据刷新后仍保留在浏览器中。

## 验收标准

- 首次打开直接进入 Demo Mode，至少展示 12 个虚构岗位。
- 搜索覆盖岗位、公司、描述和技能；无结果时可一键清除条件。
- 方向、城市、最低匹配度、只看校招筛选可组合使用。
- 每个岗位显示 0–100 匹配分，详情显示不超过 3 个理由和 2 个能力缺口。
- 收藏、投递状态和偏好写入 `localStorage`，刷新后不丢失。
- 收藏页只展示已收藏岗位，取消收藏不删除投递记录。
- 看板支持状态下拉更新，桌面为多列、移动端为分组列表。
- 官方投递使用新标签页及 `noopener noreferrer`；演示链接明确标记为虚构。
- 桌面端和移动端无明显横向溢出，按钮可用键盘操作并具有可见标签或 aria-label。

## v0 边界

- 单用户、本地优先，不登录、不存真实简历或隐私信息。
- 岗位均为虚构演示数据，不代表真实招聘。
- 不实现真实抓取、Supabase、邮件提醒、拖拽看板或自动投递。
- 后续版本通过 Repository 接口替换为 Supabase，并接入合规的公开 API/CSV 导入。

## 视觉方向

选择“安静的专业工作台”：暖灰背景、深海军蓝文字、低饱和蓝与薄荷绿状态色；采用清晰的信息层级和紧凑卡片，参考 Linear 的克制密度、Notion 的留白及现代 ATS 的看板结构。
