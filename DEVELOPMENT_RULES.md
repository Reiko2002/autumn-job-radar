# Development Rules

1. 单文件保持聚焦；领域类型、数据、业务逻辑、组件分离。
2. 匹配与筛选使用纯函数，不在组件中复制业务规则。
3. 用户数据只存本地；不得提交真实密钥，真实 `.env*` 必须忽略。
4. 新增行为必须覆盖空状态和可键盘操作路径。
5. 每个切片完成后运行 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build`。
6. Demo 公司和岗位必须是虚构内容，并在 UI 中清晰说明。
